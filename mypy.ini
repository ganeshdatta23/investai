[mypy]
# Third-party libs (pandas, numpy, yaml, requests) ship without type stubs here;
# treat their imports as Any rather than erroring. This is a research/app project,
# not a typed library.
ignore_missing_imports = True
