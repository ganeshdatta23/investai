# envs
.venv/
.conda/
__pycache__/
*.pyc
.pytest_cache/

# secrets
.env

# research data + caches + secrets + logs (NOT committed; prices.db is ~1GB)
# NOTE: leading slash anchors this to the REPO ROOT only — must not match the
# source package investai/data/.
/data/

# NOTE: state/ (paper ledger) and docs/ (dashboard) ARE tracked so the deploy persists.
docs/state.json
